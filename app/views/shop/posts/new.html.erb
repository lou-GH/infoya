<div class="container px-5 px-sm-0">
  <div class="row">

    <div class="col-md-2">
      <div class="lead font-weight-bold">新規投稿</div>
    </div>
  </div>

    <% if @post.errors.any? %>
      <%= @post.errors.count %>errors prohibited this obj from being saved:
      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>

    <%= form_with model: @post, url: shop_posts_path, method: :post do |f| %>

    <div class="row">
      <div class="col-md-9 mx-auto">

        <div class="form-group">
          <div class="col-md-12">
            <%= f.label :introduction,"投稿内容" %>
          </div>
          <div class="offset-md-1 col-md-11">
            <%= f.text_area :introduction, class: 'form-control', rows: 5 %>
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-12">
            <%= f.label :location_id, "登録地から選択" %><br>
            <small>
              ※選択肢がない場合はマイページより登録してください。
            </small>
          </div>
          <div class="offset-md-1 col-md-11">
            <%= f.select :location_id, options_from_collection_for_select(current_manufacturer.locations, :id, :name), class: 'form-control' %><br>
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-12">
            <%= f.label :genre_name, "タグ　例：野菜 果物 加工食品" %><br>
            <small>※スペースを入力することで複数のタグをつけることができます。</small>
          </div>
          <div class="offset-md-1 col-md-11">
            <%= f.text_field :genre_name, class: 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-12">
            <%= f.label :post_image,"投稿画像" %>
          </div>
          <div class="offset-md-1 col-md-11">
            <%= f.file_field :post_image %>
          </div>
        </div>



        <div class="row">
          <div class="col-md-6 mx-auto">
            <%= f.submit "投稿", class: "btn btn-primary" %>
          </div>
        </div>

      </div>
    </div>
    <% end %>

  </div>
</div>