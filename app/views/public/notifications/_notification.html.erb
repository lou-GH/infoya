<% visiter = notification.visiter %>
<% post = notification.post %>

<div class="user-view clearfix ">
    <strong>
      <%= link_to public_manufacturer_path(visiter) do %>
        <%#= image_tag visiter, :manufacturer_icon, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50", class:"profile-img-circle" %>
        <%=visiter.account_name %>
      <% end %>
    </strong>
    さんが
    <strong>
      <%= link_to public_post_path(notification.post_id) do %>
        <%= post.location.name.truncate(20)%>
      <% end %>
    </strong>
    に出荷しました。
    <%= " (#{time_ago_in_words(notification.created_at)} 前)" %>
  </li>
</ul>
